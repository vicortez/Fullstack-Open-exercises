# Part 6: Anecdotes react query

This project was initially seeded with the code from exercise 6.19.

Since the redux version was somewhat suited for production use, I decided to leave the Redux version for reference, and fork the implementation with React Query instead of replacing the redux one.
